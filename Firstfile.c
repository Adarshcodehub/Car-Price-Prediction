#include<stdio.h>
#include<conio.h>

int main()
{
int a,b[20],i;
printf("Enter the Number: ");
scanf("%d %s",&a,b);
for(i=0;i<20;i++)
{
	for(int k=0;i<20;j++)
	{
	  printf("%d is the number which is typed by %c",a[k],b[i]);
    }
}
return 0;
}
